#Calculate: Area of a Room (Area=Width*Lenght)

print("Program to calculate the aerea of a room in meters and remember - also 'float numbers' are allowed!")

width_room = float(input("Please, enter the width of the room in meters (m): "))

lenght_room = float(input("And now enter the lenght of the room in meters (m): "))

area_room = float(width_room * lenght_room)

print(f"Tha area of a room - with a width of {width_room} m and with a lenght of {lenght_room} m - is {area_room} m")

#Mispellings removed