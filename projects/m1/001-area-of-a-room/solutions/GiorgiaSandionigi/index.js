//inserisco variabili + utilizzo "prompt" per far iserire dati all'utente

let roomWidth = prompt("enter measure Width in meter")
let roomLenght = prompt("enter measure Lenght in meter")
let totalArea = roomWidth * roomLenght


//utilizzo "alert" per dare messaggio all'utente


alert ( 'the area of the room is ${totalArea} m²')