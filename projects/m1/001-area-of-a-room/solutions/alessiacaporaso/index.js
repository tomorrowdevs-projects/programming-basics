//prompt length and width. + convert string in number
const length = parseInt(prompt("Enter the length of room"));
const width = parseInt(prompt("Enter the width of room"));
//print area of room
const area = length * width;
alert("Area is " + area);