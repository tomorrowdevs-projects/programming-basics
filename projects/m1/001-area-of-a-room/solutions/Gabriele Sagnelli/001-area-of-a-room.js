//area of a room-m1-001

let require = alert('calculate room area in square meters');
let width = prompt('insert width');
let length = prompt ('insert length');
alert(`the room area is ${width * length} square meters`);
