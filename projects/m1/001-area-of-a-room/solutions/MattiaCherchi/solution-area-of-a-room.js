let roomLenght = prompt('Enter the Lenght of the room in meters');
let roomWidth = prompt('Enter the Width of the room in meters');

let roomArea = (roomLenght) * (roomWidth);

alert(`The area of the room is ${roomArea} meters`);